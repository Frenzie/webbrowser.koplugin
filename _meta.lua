local _ = require("gettext")
return {
    name = "webbrowser",
    fullname = _("Web browser (MuPDF)"),
    description = _([[Open a URL, fetch the page, and render it with MuPDF. Basic CSS and images are supported.]]),
}
